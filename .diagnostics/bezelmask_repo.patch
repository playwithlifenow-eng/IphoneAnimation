diff --git a/src/CrossSection3DScrollGLB.jsx b/src/CrossSection3DScrollGLB.jsx
index 8196ff5..0a6191c 100644
--- a/src/CrossSection3DScrollGLB.jsx
+++ b/src/CrossSection3DScrollGLB.jsx
@@ -8874,6 +8874,7 @@ function BezelMaskDebugPass({ bezelMeshes }) {
   const maskLayerRef = useRef(null);
   const originalLayerMasksRef = useRef([]);
   const targetSizeRef = useRef(new THREE.Vector2());
+  const lastAuditPostRef = useRef("");
 
   const resources = useMemo(() => {
     const target = new THREE.WebGLRenderTarget(1, 1, {
@@ -9128,6 +9129,11 @@ function BezelMaskDebugPass({ bezelMeshes }) {
         audit.height = size.y;
         audit.samples = samples;
         audit.status = "READY";
+        const signature = `${Number(audit.progress).toFixed(6)}:${size.x}:${size.y}:${samples}:${audit.status}`;
+        if (signature !== lastAuditPostRef.current && window.parent && window.parent !== window) {
+          lastAuditPostRef.current = signature;
+          window.parent.postMessage({ type: "bezel-mask-audit", audit }, "*");
+        }
       }
     } finally {
       scene.overrideMaterial = previousOverride;
